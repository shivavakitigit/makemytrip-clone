import * as React from "react";
import Stack from "@mui/material/Stack";
import Button from "@mui/material/Button";

export default function CustomButton({ variant, color }) {
  debugger;
  return (
    <Button variant={variant} color={color}>
      Contained
    </Button>
  );
}
